<p class="form-field">
  <%= tag.label device.label, name: "mirror[device_ids]", for: device.id, class: :key %>
  <%= tag.input id: device.id,
                type: :checkbox,
                name: "mirror[device_ids][]",
                value: device.id,
                checked: (device.playlist_id == playlist_id) %>
</p>
