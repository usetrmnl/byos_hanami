<input type="submit"
       value="Save"
       class="button-accept"
       <%= "hx-#{verb}=#{path}" %>
       hx-trigger="click, keyup[ctrlKey&&key=='s'] from:closest .item"
       hx-target="closest .page-form"
       hx-swap="outerHTML"
       hx-push-url="true">
