<input type="submit"
       value="Save"
       class="button-accept"
       <%= "hx-#{verb}=#{path}" %>
       hx-trigger="click, keyup[ctrlKey&&key=='s'] from:closest .model"
       hx-target="closest .page-models"
       hx-swap="outerHTML"
       hx-push-url="true">
