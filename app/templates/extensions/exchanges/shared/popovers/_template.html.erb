<%= scope(:popover_default_content, name: :template, label: "Template").render do %>
  <p>The list of URIs for making HTTP requests to acquire source data for your extension. Each URI must be delimited by a new line. <%= link_to "Liquid", "https://shopify.github.io/liquid" %> syntax is supported.</p>
<% end %>
