<%= scope(:popover_default_content, name: :body, label: "Body").render do %>
  <p>The HTTP request body. For use in HTTP POST requests. Ignored for HTTP GET requests. <%= link_to "Liquid", "https://shopify.github.io/liquid" %> syntax is supported.</p>
<% end %>
